# HW 2. Агрегаты, GROUP BY и подзапросы

17 задач, **20 баллов**. Q01–Q14 – по 1 баллу, Q15–Q17 – по 2 балла.

Сдавать файл **обязательно** с именем из почты `@edu.hse.ru`:

- `avivanov@edu.hse.ru` → `avivanov_hw2.sql`
- `avivanov_1@edu.hse.ru` → `avivanov_1_hw2.sql`

**Проверка приёма.** Для задач Q12, Q14, Q15 недостаточно совпасть по результату: мы будем проверять, что в блоке есть требуемый **CTE** (`WITH ... AS (...)`) и / или **подзапрос** (`(SELECT ...)`), как в таблице и в тексте задачи.


| Задача | Что должно быть в SQL |
| ------ | --------------------- |
| Q12    | подзапрос в `HAVING`  |
| Q14    | подзапрос во `FROM`   |
| Q15    | CTE `WITH ... AS`     |


---



### Q01, 1 балл

Посчитайте, сколько рейсов в таблице `flights`. Одна строка результата, столбец `flight_count`. `GROUP BY` не нужен.

### Q02, 1 балл

По таблице `flights` найдите самый ранний и самый поздний **плановый** вылет. Одна строка, столбцы `earliest_departure` и `latest_departure` (`min` и `max` по `scheduled_departure`).

### Q03, 1 балл

В таблице `segments` возьмите строки с `flight_id <= 5000` и по этому срезу верните одну строку: `segment_count` (число сегментов), `avg_price` (средняя цена, округлите до двух знаков), `min_price`, `max_price`. `GROUP BY` не нужен.

### Q04, 1 балл

Определите, сколько рейсов в таблице `flights` приходится на каждый статус. Выведите `status` и `flight_count`. Отсортируйте по убыванию `flight_count`, при равенстве – по `status`.

### Q05, 1 балл

По таблице `segments` для каждого класса тарифа выведите `fare_conditions`, `segment_count`, среднюю цену `avg_price` (округлите до двух знаков) и `max_price`. Отсортируйте по `fare_conditions`.

### Q06, 1 балл

По таблице `airports` для каждой страны выведите `country` и число аэропортов `airport_count`. Оставьте страны, где аэропортов не меньше 30. Отсортируйте по убыванию `airport_count`, при равенстве – по `country`.

### Q07, 1 балл

В таблице `timetable` сгруппируйте рейсы по паре аэропортов отправления и прибытия. Выведите `departure_airport`, `arrival_airport` и число рейсов `flight_count`. Оставьте направления, где рейсов строго больше 100.

Отсортируйте по убыванию `flight_count`, при равенстве – по `departure_airport` по возрастанию, затем по `arrival_airport` по возрастанию.

### Q08, 1 балл

В таблице `timetable` найдите рейсы со статусом `Scheduled`, у которых `scheduled_departure` строго позже `bookings.now()`. Для каждого аэропорта отправления выведите `departure_airport` и число таких рейсов `scheduled_count`. Оставьте аэропорты, где `scheduled_count` не меньше 15.

Отсортируйте по убыванию `scheduled_count`, при равенстве – по `departure_airport`.

### Q09, 1 балл

По таблице `flights` для каждого `status` выведите `status`, `flight_count` и самый поздний плановый вылет в группе как `latest_scheduled_departure` (`max` по `scheduled_departure`). Отсортируйте по убыванию `flight_count`, при равенстве – по `status`.

### Q10, 1 балл

В таблице `flights` приведите `scheduled_departure` к календарной дате `departure_date` и посчитайте число рейсов `flight_count` на каждый день. Выведите `departure_date`, `flight_count`. Оставьте дни, где рейсов не меньше 192.

Отсортируйте по убыванию `flight_count`, при равенстве – по `departure_date`.

### Q11, 1 балл

По таблице `flights` для каждого `status` выведите `status`, общее число рейсов `flight_count` и число рейсов с известным `actual_departure` как `departed_count`. Второй счётчик посчитайте агрегатом с `FILTER`. Отсортируйте по `status`.

### Q12, 1 балл

По таблице `segments` для каждого `fare_conditions` выведите `fare_conditions` и среднюю цену `avg_price` (округлите до двух знаков). 

Оставьте только классы, чья средняя цена строго выше общей средней по всей таблице. 

Порог – подзапрос в `HAVING`; округление только в выводимом столбце. Отсортируйте по убыванию `avg_price`, при равенстве – по `fare_conditions`.

### Q13, 1 балл

В таблице `segments` среди строк с `flight_id <= 5000` для каждого билета посчитайте число сегментов. Выведите `ticket_no` и `segment_count` для билетов с ровно четырьмя сегментами и суммой цен строго больше 220000. Отсортируйте по `ticket_no`.

### Q14, 1 балл

Во вложенном запросе во `FROM` по `segments` с `flight_id <= 5000` для каждого `ticket_no` посчитайте сумму `price`.

Снаружи по этим суммам верните одну строку: среднее `avg_ticket_price` (округлите до двух знаков), минимум `min_ticket_price` и максимум `max_ticket_price`.

### Q15, 2 балла

В таблице `tickets` оставьте бронирования с `book_ref < '100000'` и для каждого `book_ref` посчитайте число билетов `ticket_count`. Выведите бронирования, у которых `ticket_count` равен максимальному среди таких групп (если несколько – все). 

Оформите через CTE (`WITH ... AS (...)`). Столбцы `book_ref`, `ticket_count`. Отсортируйте по убыванию `ticket_count`, при равенстве – по `book_ref`.

### Q16, 2 балла

В таблице `timetable` для прибывших рейсов (`status = 'Arrived'`) сгруппируйте по аэропорту отправления. Выведите `departure_airport`, число таких рейсов `arrived_count` и среднее `actual_arrival - scheduled_arrival` как `avg_delay` (это `interval`). 

Оставьте аэропорты, где прибывших не меньше 100. Отсортируйте по убыванию `avg_delay`, при равенстве – по `departure_airport`.

### Q17, 2 балла

В таблице `segments` среди строк с `flight_id <= 5000` для каждого билета склейте классы тарифа через `string_agg` с разделителем `', '` (без `DISTINCT`) (внутри агрегата значения по возрастанию `fare_conditions`) и посчитайте сумму цен. 

Выведите `ticket_no`, `fare_classes` и `total_price` для билетов с суммой строго больше 250000. Отсортируйте по убыванию `total_price`, затем по `ticket_no`.