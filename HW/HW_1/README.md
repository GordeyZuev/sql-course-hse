# HW 1. SELECT, фильтрация, выражения и сортировка

19 задач, **20 баллов**. Q01–Q10 – по 1 баллу, Q11–Q18 – по 1,1, Q19 – 1,2.

Сдавать файл **обязательно** с именем из почты `@edu.hse.ru`: 
- `avivanov@edu.hse.ru` → `avivanov_hw1.sql`
- `avivanov_1@edu.hse.ru` → `avivanov_1_hw1.sql`

### Q01, 1 балл

Выведите из таблицы `airplanes` все столбцы (`airplane_code`, `model`, `range`, `speed`) и отсортируйте строки по `airplane_code`.

### Q02, 1 балл

Выведите из таблицы `airplanes` код, модель и дальность (`airplane_code`, `model`, `range`) и отсортируйте строки по `airplane_code`.

### Q03, 1 балл

Выведите из таблицы `airplanes` столбцы `airplane_code`, `model` и `range`, упорядочив типы по возрастанию дальности; при равной дальности сортируйте по `airplane_code`.

### Q04, 1 балл

Определите три типа самолётов с наибольшей дальностью полёта из таблицы `airplanes`: столбцы `airplane_code`, `model`, `range`. Сначала большая дальность, при равенстве – по `airplane_code`.

### Q05, 1 балл

Покажите первые десять аэропортов из таблицы `airports`: код, название и город (`airport_code`, `airport_name`, `city`). Отсортируйте вывод по `airport_code`.

### Q06, 1 балл

Покажите аэропорты с 11-й по 20-ю строку после сортировки по коду. Из таблицы `airports` возьмите столбцы `airport_code`, `airport_name`, `city`, отсортируйте по `airport_code`, пропустите первые десять строк и выведите следующие десять.

### Q07, 1 балл

Выведите из таблицы `airports` столбцы `airport_code`, `city` и `country`. Сначала страны идут по алфавиту от A к Z; внутри одной страны города – наоборот, от Z к A; если город совпал – по `airport_code`. Оставьте первые 20 строк.

### Q08, 1 балл

Определите аэропорты Германии из таблицы `airports` (страна `Germany`): столбцы `airport_code`, `airport_name`, `city`. Отсортируйте по городу, затем по коду.

### Q09, 1 балл

Найдите в таблице `airplanes` типы с дальностью не меньше 4000 км: столбцы `airplane_code`, `model`, `range`. Отсортируйте по убыванию дальности, при равенстве – по `airplane_code`.

### Q10, 1 балл

Определите аэропорты Москвы в России из таблицы `airports`: столбцы `airport_code`, `airport_name`, `city`, `timezone`. Нужны строки со страной `Russia` и городом `Moscow`. Отсортируйте по `airport_code`.

### Q11, 1,1 балла

Найдите в таблице `airplanes` типы с дальностью от 2000 до 8000 км включительно: столбцы `airplane_code`, `model`, `range`, `speed`. Отсортируйте по возрастанию дальности, при равенстве – по `airplane_code`.

### Q12, 1,1 балла

Определите аэропорты Франции, Италии и Испании из таблицы `airports` (страны `France`, `Italy` и `Spain`): столбцы `airport_code`, `city`, `country`. Отсортируйте по стране, городу и коду.

### Q13, 1,1 балла

Определите, какие статусы встречаются в таблице `flights`. Выведите столбец `status` без повторов и отсортируйте по алфавиту.

### Q14, 1,1 балла

Покажите рейсы из таблицы `flights`, у которых:
- плановый вылет в тот же календарный день, что у `bookings.now()`;
- этот вылет не раньше `bookings.now()` и при этом раньше, чем через три часа от него.

Выведите `flight_id`, `route_no`, `scheduled_departure` и день вылета `departure_date`. Отсортируйте по `scheduled_departure`, затем по `flight_id`.

### Q15, 1,1 балла

Выведите из таблицы `airports_data` аэропорты, у которых английское имя города – `Moscow`. Нужны столбцы `airport_code`, русское название аэропорта `name_ru` и русское название города `city_ru`. Отсортируйте по `airport_code`.

### Q16, 1,1 балла

Найдите в таблице `flights` рейсы, которые уже вылетели, но ещё не прибыли (подумать как это определить). Выведите `flight_id`, `route_no`, `actual_departure`, `actual_arrival` и отсортируйте по `actual_departure`, затем по `flight_id`.

### Q17, 1,1 балла

Выберите из таблицы `tickets` билеты «туда» (`outbound` истинно), у которых полное имя (вся строка) `passenger_name` **начинается с `My`** без учёта регистра. Столбцы `ticket_no`, `passenger_id`, `passenger_name`. Отсортируйте по имени пассажира, затем по номеру билета.

### Q18, 1,1 балла

Разметьте типы из таблицы `airplanes` по дальности. Выведите `airplane_code`, `model`, `range` и столбец `haul`: `short`, если дальность меньше 3000; иначе `medium`, если меньше 8000; иначе `long`. Отсортируйте по убыванию дальности, затем по `airplane_code`.

### Q19, 1,2 балла

Найдите в таблице `timetable` ещё не вылетевшие длинные рейсы аэропорта «Внуково»: статус `Scheduled`, вылет не раньше `bookings.now()`, хотя бы один конец маршрута – `VKO`, длительность строго больше трёх часов.
Выведите `flight_id`, `route_no`, `departure_airport`, `arrival_airport`, `scheduled_departure` и эту длительность как `planned_duration`. Отсортируйте по убыванию длительности, затем по `flight_id`, и оставьте не больше 50 строк.
