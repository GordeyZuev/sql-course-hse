## **О курсе**

Курс читается для студентов 1 курса магистратуры «Искусственный интеллект» в 1 модуле 2026/2027 учебного года в качестве «Семинара наставника».

Курс направлен на изучение языка SQL. Изучение представляет из себя набор теории и практик, позволяющих писать запросы к базам данных (БД) и использовать стандартные функции обработки данных.

### **План курса**


| № Занятия | Дата     | Тема                                          |
| --------- | -------- | --------------------------------------------- |
| 1         | 03.09.26 | Вступление, основные операторы. Типы данных.  |
| 2         | 07.09.26 | Группировки и агрегатные функции. Подзапросы. |
| 3         | 11.09.26 | Объединение таблиц и множества. CTE.          |
| 4         | 18.09.26 | Оконные функции. DML / DDL операции.          |




### **Подробное описание пар**



#### **Пара 1 – «Введение» (03.09.26)**

**Цель:** научиться читать данные из одной таблицы.

- Введение: зачем SQL в ИИ-проектах; таблица, столбец, строка.
- Операторы выборки: `SELECT`, `FROM`, `WHERE`, `ORDER BY`, `LIMIT`.
- Типы данных; `NULL` и `IS NULL`.
- Фильтрация: `AND` / `OR`, `IN`, `BETWEEN`, `LIKE`.

---



#### **Пара 2 – «Сжимаем и группируем» (07.09.26)**

**Цель:** агрегировать данные и фильтровать группы.

- `GROUP BY`; агрегатные функции: `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`.
- `HAVING` vs `WHERE` – на простом примере.
- Простые подзапросы: scalar в `WHERE`, `IN (...)`.

---



#### **Пара 3 – «Склеиваем таблицы» (11.09.26)**

**Цель:** объединять данные из нескольких таблиц.

- `INNER JOIN`, `LEFT JOIN` – когда какой использовать.
- CTE (`WITH ... AS`) – многошаговые запросы.
- `UNION ALL` – склеить два одинаковых по структуре `SELECT`.

---



#### **Пара 4 – «Анализируем и сохраняем» (18.09.26)**

**Цель:** научиться применять оконные функции и сохранять результаты запросов в БД.

**Оконные функции:**

- `ROW_NUMBER`, `RANK`, `DENSE_RANK`.
- `SUM(...) OVER (PARTITION BY ... ORDER BY ...)`.
- `LAG` / `LEAD` – один пример, если успеваем.

**DML и DDL:**

- `INSERT ... SELECT`.
- `CREATE TABLE AS SELECT`.
- `UPDATE` / `DELETE` с `WHERE`.
- `BEGIN` / `COMMIT` / `ROLLBACK`.

---



### **Домашние задания**

Четыре небольших HW на закрепление материала – **выкладываются сразу** в начале курса. Сдавать рекомендуется по мере прохождения. Все HW имеют **одинаковый вес**.

1. HW 1 – фильтрация и сортировка данных по одной таблице
2. HW 2 – группировки, агрегатные функции, простые подзапросы
3. HW 3 – объединение таблиц, CTE
4. HW 4 – оконные функции; сохранение результатов запроса в БД

---



### **Организационное**

*Для преподавателей и организаторов.*


| ID   | Тема                                         | Формат                | Статус    |
| ---- | -------------------------------------------- | --------------------- | --------- |
| HW 1 | Фильтрация и сортировка по одной таблице     | `.sql` / Контест      | не начато |
| HW 2 | GROUP BY, агрегаты, простые подзапросы       | `.sql` / Контест      | не начато |
| HW 3 | JOIN, CTE                                    | `.sql` / Контест      | не начато |
| HW 4 | Оконные функции; сохранение результатов в БД | `.sql` / Контест, DML | не начато |




### **Формула оценки**

Оценка бинарная – зачёт / незачёт. Ставится в рамках дисциплины «Семинар наставника» (1-ый модуль).

ИТОГ = `Зачет IF AVG(HW_1, HW_2, HW_3, HW_4) > 3.5 ELSE Незачет`

### **Ассистенты (TBD)**

–
